openai>=1.12.0
apscheduler>=3.10.4
httpx>=0.27.0
feedparser>=6.0.10
python-dotenv>=1.0.0
flask>=3.0.0
requests>=2.31.0
pyyaml>=6.0.1
easyocr>=1.7.0
pillow>=10.0.0
# Canva API (optional) - for professional branded image generation
# Note: Canva API is currently in beta, adjust as needed when official SDK is released

# v3.0: Headless LinkedIn Automation
selenium>=4.15.0
webdriver-manager>=4.0.1

# v3.1: LinkedIn Official API + OAuth Helper
# Note: Flask only needed for linkedin_oauth.py (one-time token generation)
# flask>=3.0.0 already included above

